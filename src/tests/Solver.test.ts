import { SolverRecursive } from '../SolverRecursive'

describe("Solver", () => {
	var solver: SolverRecursive;
	describe("Solver", () => {
		test('Read input', async () => {
		// 	solver = new Solver();
		// 	await solver.read();
		// 	expect(solver.bitmap).toEqual('1\n\n3 4\n0001\n0011\n0110\n');
		// });
		// test('Solution to example test case"', async () => {
		// 	console.log = jest.fn();
		// 	await solver.start();
		// 	var solution = [[3, 2, 1, 0], [2, 1, 0, 0],[1, 0, 0, 1]];

		// 	for (const column of solution) {
		// 		expect(console.log).toHaveBeenCalledWith(...column);
		// 	}
		// });
		// test('Solution to 1 in the middle"', async () => {
		// 	console.log = jest.fn();
		// 	await solver.start();
		// 	var solution = [[3, 2, 1, 0], [2, 1, 0, 0],[1, 0, 0, 1]];

		// 	for (const column of solution) {
		// 		expect(console.log).toHaveBeenCalledWith(...column);
		// 	}
		});
	})
})
